name: 'Merginator 2'
description: 'This Action merges pull requests and deletes the branch associated with the PR.'
runs:
   using: 'docker'
   image: 'Dockerfile'
branding:
   icon: 'git-merge'
   color: 'orange'
